pipeline {
    agent any
    stages {
        stage('BUILD') {
            steps {
                echo "Pipeline depuis Jenkinsfile"
                sh "mvn -version"
            }
        }
    }
}
